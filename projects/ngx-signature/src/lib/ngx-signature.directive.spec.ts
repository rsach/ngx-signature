import { NgxSignatureDirective } from './ngx-signature.directive';

describe('NgxSignatureDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxSignatureDirective();
    expect(directive).toBeTruthy();
  });
});
